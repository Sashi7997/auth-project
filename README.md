# Full Stack Auth App (MFA + Docker)

## Features
- Login + Register
- MFA (OTP Authentication)
- PostgreSQL Database
- Dockerized Full Stack

## Run Project

1. Install Docker
2. Run:
   docker-compose up --build
3. Open:
   http://localhost:3000
