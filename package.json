{
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "db:setup": "node scripts/apply-training-schema.js",
    "db:prisma-init": "npm run prisma:generate --prefix backend && npm run prisma:migrate --prefix backend -- --name init",
    "db:prisma-deploy": "npm run prisma:generate --prefix backend && npx prisma migrate deploy --schema backend/prisma/schema.prisma",
    "dev": "npm run dev --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend"
  },
  "dependencies": {
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
